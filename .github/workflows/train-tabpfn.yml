name: Arenix Train TabPFN
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  train:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - run: pip install -r requirements.txt
      - name: Train leakage-safe NFL TabPFN challenger
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
        run: python src/train_tabpfn.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: arenix-tabpfn-0.1.0
          path: artifacts/
          if-no-files-found: warn
          retention-days: 30
